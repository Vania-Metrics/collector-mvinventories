# Inventory switch metrics.
#
# As much a gameplay indicator as a load indicator: every switch reads and writes a profile to disk.
name: VaniaMetrics-Inventories
version: ${version}
main: fr.samflix.vaniametrics.module.mvinventories.InventoriesPaper
api-version: '1.21'
authors: [mc-vania]
description: "Multiverse-Inventories metrics: inventory switches."
depend: [VaniaMetrics, Multiverse-Inventories]
